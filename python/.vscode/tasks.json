{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "hdfs dfs -rm -r -f /output && ${command:python.interpreterPath} invertedIndex.py",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        },
    ]
}